import { css } from "@emotion/react"

const norm = css`
  button,
  input[type="button"] {
    cursor: pointer;
    border-width: 1px;
    border-radius: 4px;
    border-style: solid;
  }
`


export default norm
