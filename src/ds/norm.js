import { css } from "@emotion/react"

const norm = css`
  button,
  input[type="button"],
  input[type="submit"] {
    cursor: pointer;
    border-width: 1px;
    border-radius: 6px;
    border-style: solid;
    padding: 0.5rem 1rem;
  }
`


export default norm
